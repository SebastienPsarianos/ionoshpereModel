spack:
  specs:
  - trilinos@16.0.0 ~fortran +mpi +shared +tpetra +zoltan2 %gcc

  packages:
    mpi:
      require:
      - openmpi +fortran
    blas:
      require:
      - netlib-lapack
    lapack:
      require:
      - netlib-lapack

  view: true
  concretizer:
    unify: true
